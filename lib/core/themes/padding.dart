import 'package:flutter/material.dart';

class ProjectPadding {
  static const pagePaddingHorizontal = EdgeInsets.symmetric(horizontal: 23);
  static const pagePaddingVertical = EdgeInsets.symmetric(vertical: 15);
  static const inputPaddingVertical = EdgeInsets.symmetric(vertical: 10);
  static const inputPaddingBottom = EdgeInsets.only(bottom: 10);
  static const pagePaddingRightOnly = EdgeInsets.only(right: 20);
  static const buttonPadding = EdgeInsets.all(4);
  static const pagePaddingAll = EdgeInsets.only(
    left: 20,
    right: 20,
    bottom: 20,
  );
  static double inputBoxHeight = 8;
}
